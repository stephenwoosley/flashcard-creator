
var BasicCard = function(front, back) {
  this.front = front;
  this.back = back;

  //the front text should console.log out as is it was entered.
  //the back text should also console.log out as the string entered.
}

module.exports = BasicCard;
